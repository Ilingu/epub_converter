mod idwtr;
mod ietclh;
mod nhad;
mod utils;

use ietclh::build_ietclh;
use nhad::build_nhad;

fn main() {
    // build_i_dont_want_this_reincarnation().unwrap();
    // build_ietclh().unwrap();
    build_nhad().unwrap()
}
